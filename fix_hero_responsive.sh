sed -i '/{siteSettings?.products_banner_url ? (/,/        )}/c\
        {(siteSettings?.products_banner_url || siteSettings?.products_banner_mobile_url) ? (\n          <motion.div\n            initial={{ opacity: 0, y: 10 }}\n            animate={{ opacity: 1, y: 0 }}\n            className="w-full mb-12 overflow-hidden rounded-2xl shadow-sm border border-slate-100 bg-slate-50"\n          >\n            {siteSettings?.products_banner_url && (\n              <img\n                src={siteSettings.products_banner_url}\n                alt="Store Banner"\n                referrerPolicy="no-referrer"\n                className={`w-full h-auto max-h-[400px] object-cover md:object-center ${siteSettings.products_banner_mobile_url ? '\''hidden sm:block'\'' : '\''block'\''}`}\n              />\n            )}\n            {siteSettings?.products_banner_mobile_url && (\n              <img\n                src={siteSettings.products_banner_mobile_url}\n                alt="Store Banner (Mobile)"\n                referrerPolicy="no-referrer"\n                className={`w-full h-auto object-cover ${siteSettings.products_banner_url ? '\''block sm:hidden'\'' : '\''block'\''}`}\n              />\n            )}\n          </motion.div>\n        ) : (\n          <div className="w-full text-center mb-12">\n            <motion.span \n              initial={{ opacity: 0, y: 10 }}\n              animate={{ opacity: 1, y: 0 }}\n              className="text-brand-pine font-black uppercase tracking-widest text-[10px] mb-4 bg-brand-cream px-3 py-1 rounded"\n            >\n              MYBOOKS365 LIBRARY\n            </motion.span>\n            <motion.h1 \n              initial={{ opacity: 0, y: 10 }}\n              animate={{ opacity: 1, y: 0 }}\n              transition={{ delay: 0.1 }}\n              className="text-3xl sm:text-4xl md:text-5xl font-bold text-[#111827] tracking-tight leading-tight mt-6 mb-4 font-sans"\n            >\n              Explore Our eBooks\n            </motion.h1>\n            <motion.p \n              initial={{ opacity: 0, y: 10 }}\n              animate={{ opacity: 1, y: 0 }}\n              transition={{ delay: 0.2 }}\n              className="text-slate-500 text-sm sm:text-base max-w-2xl mx-auto font-medium"\n            >\n              Explore practical digital books covering business, entrepreneurship, marketing, finance, productivity, AI and growth.\n            </motion.p>\n          </div>\n        )}' src/pages/ProductsPage.tsx
